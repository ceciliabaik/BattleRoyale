package pokerroyale.controller;

import pokerroyale.model.Game;
import pokerroyale.view.ConsoleView;

public class GameController {

  public boolean play(Game game, ConsoleView view) {
    view.displayWelcomeMessage();

    if (game.isGameOver()) {
      }
    }
}
